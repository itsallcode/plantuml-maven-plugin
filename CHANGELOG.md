# Changelog

## 0.2.0

- support the jeluard-compatible `sourceFiles`, `format`, and `outputDirectory` configuration model
- preserve relative source paths below the output directory for rendered diagrams
- add integration coverage for source selection and output path preservation
